#include "../../includes/minishell.h"

void	add_redir(t_cmd *cmd, t_token_type type, char *value)
{
	t_redir *new;

	// new = malloc(sizeof(t_redir));
	// if (!new)
	// 	return ;
	// new->type = type;
	// new->file = strdup(value);
	// new->fd = -1;
	// new->next = cmd->redir;
	// cmd->redir = new;
}